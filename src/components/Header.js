import React from "react";
import "./../index.css";

const Header = props => {
    return(
        <header>
            <h1 className="header">MyReads</h1>
        </header>
    );
}

export default Header;
